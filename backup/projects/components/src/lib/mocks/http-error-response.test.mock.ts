import { HttpErrorResponse } from "@angular/common/http";

const HTTP_ERROR_RESPONSE = {
    error: {
        message: 'error message'
    }
} as any as HttpErrorResponse;

export default HTTP_ERROR_RESPONSE;
